from json import dumps, loads

# Returns a map segment centered around a x/y couple
def generateMap(x,y):
    map = [0 for i in range(25*25)]
    return map

# Returns a list of (x,y,type,data as json)
def generateRessources(bounds,typeDensity = {0:1,1:0.5,2:0.25,3:0.125}):
    r = []

    return r
